操作目录：
1 窗体多种打开方式
2 Bundle在Activity间数据传递

1 窗体多种打开方式
要求：
（1）多种窗体打开方式
（2）Activity Result API
2  Bundle在Activity间数据传递
要求：
（1）在窗体1中提交数据，在窗口2中显示。

2 FragmentTabHost和FragmentActivity实现应用主界面
要求：
（1）能够实现应用界面的Tab切换
·用 FragmentTabHost 管理 Tab 与 Fragment 的映射关系
·每个 Tab 对应一个独立 Fragment（新闻、阅读、视频、发现、我的）
·点击 Tab 时自动切换显示对应 Fragment
（2）改进：从网上找个新闻数据接口，从网上加载数据

操作目录：
1 Fragment生命周期
2 FragmentTabHost和FragmentActivity实现应用主界面

1 Fragment生命周期
要求：
（1）显示Fragment生命周期
（2）动态加载Fragment，静态加载Fragment、跳转
